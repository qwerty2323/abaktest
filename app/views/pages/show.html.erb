<p id="notice"><%= notice %></p>
<div class="child-pages">
  <h4>Child pages:</h4>
  <%= render( partial: 'child_pages', locals: { pages:  Page.where('parent = ?', @page.name) }) if Page.where('parent = ?', @page.name) %>
  <hr>
</div>
<h4><%= @page.title %></h4>
<br>
<%= @page.name %>
<br>Content:<br>
<%= prepare_content(@page.content).html_safe %>
<br>ID:<br>
<%= @page.id %>
<br>

<%= link_to 'Edit', update_page_path({pages: generate_path(@page)}) %> |
<%= link_to 'Root', root_path %>
